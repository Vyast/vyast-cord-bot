import { type CacheType, type StringSelectMenuInteraction } from "discord.js";

export const stringSelectMenuInteraction = async (
  _interaction: StringSelectMenuInteraction<CacheType>
): Promise<void> => {
  return;
};
